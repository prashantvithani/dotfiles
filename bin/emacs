#!/bin/bash

# EMACS="$HOME/Workspace/repos/emacs/nextstep/Emacs.app/Contents/MacOS/Emacs"

if [[ -n "$EMACS_VERSION" ]]; then
    EMACS="$HOME/Workspace/repos/emacs-$EMACS_VERSION/src/emacs"
else
    EMACS="$HOME/Workspace/repos/emacs/src/emacs"
fi

# EMACS="/usr/bin/emacs"
exec "$EMACS" "$@"
